(function () {
    'use strict';

    angular
        .module('courseApp')
        .controller('createController', createCourse);

    function createCourse($scope, $http) {

        $scope.createCourse = function () {

        }
    }
})();
